import styled from "styled-components";

export const BookingArticleBox = styled.section`
  padding: 3.687rem 0;
  padding-bottom: 0;
  > div {
    text-align: center;
    max-width: 34rem;
    margin: 0 auto;
    > p {
      color: var(--col-40);
      margin-bottom: 0.5rem;
    }

    article {
      h2 {
        color: var(--col-30);
        font-size: 1.5rem;
        font-weight: 600;
        line-height: 120%;
      }

      p {
      }
    }
  }
`;
